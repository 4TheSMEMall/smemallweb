[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install -g pnpm@11.5.2", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @sme-mall/api db:generate",
  "pnpm --filter @sme-mall/api build"
]

[start]
cmd = "node apps/api/dist/server.js"
